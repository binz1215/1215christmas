import { inputMenu } from "../view/inputView";

class ClassName {
  #object_XY;

  async valid_XYZ(){
    let valid = true;
    
    while (valid) {
      try {
        this.#object_XY = await input_XYZ();
        this.#_XY_ValidCheck();
        valid = false;
      } catch (error) {
        
      }
    }
    return this.#object_XY;
  }

  #_XY_ValidCheck(){
    if(this.#) {
      Console.print(`[ERROR] `);
      throw new Error('[ERROR]')
    }
    if(this.#) {
      Console.print(`[ERROR] `);
      throw new Error('[ERROR]')
    }
  }

  #is_XY() {
    if() return true;
  }

}
export default ClassName;

const play = new ClassName();
const x = play.
//
console.log(x);